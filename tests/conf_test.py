#import pytest

# Set default async mode for pytest-asyncio
# Ensures pytest-asyncio automatically detects and runs async functions
#@pytest.fixture(scope="session", autouse=True)
#def configure_asyncio():
#    pytest.asyncio_mode = "auto"
